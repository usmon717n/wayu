export enum QuestionStatus {
  Pending = 'pending',
  Answered = 'answered',
  Repeated = 'repeated',
  Rejected = 'rejected',
}

export enum VacancyType {
  FullTime = 'fullTime',
  PartTime = 'partTime',
}

export enum ApplicationStatus {
  Active = 'active',
  Accepted = 'accepted',
  Rejected = 'rejected',
}
